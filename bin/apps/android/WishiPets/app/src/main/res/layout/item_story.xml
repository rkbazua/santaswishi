<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
                name="story"
                type="com.rbazua.wishipets.model.Story"/>
        <variable
                name="listener"
                type="com.rbazua.wishipets.view.StoryClickListener" />
    </data>

    <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:onClick="@{listener::onStoryClicked}"
            android:layout_height="125dp">
        
        <TextView
                android:id="@+id/storyId"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:text="@{String.valueOf(story.petuid)}"/>

        <ImageView
                android:id="@+id/storyPicture"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="2"
                android:imageUrl="@{String.valueOf(story.petuid)}"
                android:padding="4dp"/>
        <LinearLayout
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="3"
                android:padding="4dp"
                android:gravity="center_vertical"
                android:orientation="vertical">
            <TextView
                    android:id="@+id/titleText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    style="@style/Title"
                    android:text="@{story.title}"/>
            <TextView
                    android:id="@+id/descriptionText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    style="@style/Text"
                    android:text="@{story.description}"/>
        </LinearLayout>

    </LinearLayout>
</layout>